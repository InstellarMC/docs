pre-commit:
  parallel: true
  commands:
    check:
      run: bun run biome check --no-errors-on-unmatched --files-ignore-unknown=true --colors=off {staged_files}
      stage_fixed: true # before committing
    tsc:
      run: bun run tsc --noEmit --skipLibCheck
